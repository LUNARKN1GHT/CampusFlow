# CampusFlow —— 校园学习事务智能助理

## 文档用途与项目状态

- 本文件保存后续设计与开发需要复用的项目背景、业务约定和阶段边界。
- [README.md](README.md) 是面向外部展示的完整项目计划书，包含详细场景、功能说明和验收标准；开展具体功能工作前应阅读相关章节。
- [CONTRIBUTING.md](CONTRIBUTING.md) 保存任务认领、分支提交、MR／PR 评审、验证与资料管理约定；参与项目修改时应遵循。项目许可证待定，确定后同步更新正式 LICENSE 和相关说明。
- 当前处于工程骨架阶段，已建立 Vue 前端、Python FastAPI 后端、健康检查和基础测试；业务功能、数据库、模型调用和队列尚未实现。具体成员分工仍待确定。
- 工程设计见 [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)，本地启动与检查见 [docs/DEVELOPMENT.md](docs/DEVELOPMENT.md)。
- 后续业务范围、阶段安排或关键规则发生变化时，同步维护本文件与 README.md；对外功能状态必须以实际实现和验证结果为准。

## 产品定位

面向学生个人学习事务，把分散的课程资料与通知整理成有依据、可执行的学习安排，回答“需要知道什么、应该先做什么、接下来怎么安排”。

基本业务闭环：导入资料 → 识别与提取 → 核对来源和适用范围 → 可追溯查询／任务整理 → 生成计划与检查冲突 → 用户确认与执行 → 更新进度和跟进变化。

用户可以独立使用查询、任务管理或规划功能，不要求每次走完整个闭环。首期以个人工作空间为单位，教师协作、班级管理和学校级教务管理另行评估。

## 应用场景

- 培养方案、课程大纲、教务通知
- 作业要求、考试时间、实验室规定
- 微信群截图、PDF、Word、网页通知
- 某门课 DDL 是什么？
- 课程之间的拓扑排序？
- 对于上述这些内容，我未来两周需要做什么？

## 可以做出的功能

- 上传课程资料、教务通知和群聊截图。
- 多模态模型识别截图、表格和 PDF。
- RAG 检索原文并给出可追溯引用。
- Agent 判断用户是在“查询信息”“制定计划”还是“检查冲突”。
- 生成学习计划、待办清单和日历草案。
- 对不确定信息明确提示，不允许凭空编造。
- 用户确认后再执行敏感操作。

## 业务模块

以下是完整功能方向，各模块按交付阶段逐步落地，不应全部纳入首版。

| 模块 | 业务范围 |
| --- | --- |
| 学期与课程 | 学期归档、课程与教学班、课程别名、专业年级、培养方案版本、已修课程、时区与可用时间 |
| 资料管理 | PDF、Word、图片、群聊截图、公开网页与粘贴文本；来源记录、处理状态、原文定位、去重、版本、归档与删除 |
| 内容提取 | 识别任务、日程、规则与课程关系；逐项查看依据、核对、修正、忽略和合并 |
| 可追溯问答 | 按课程、学期和时间检索；连续追问、多资料对照、引用定位、缺失信息与冲突说明 |
| 任务管理 | 候选任务与手动任务、优先级、耗时、子任务、依赖、进度和调整记录 |
| 学习计划 | 按日、周、未来两周或自定义范围生成草案；工作量、时间块、安排理由、未排入事项、锁定安排与局部重排 |
| 冲突检查 | 固定日程重叠、截止风险、依赖冲突、来源矛盾与学习负荷风险 |
| 课程路径 | 强制先修、并修与建议先学；已修状态、依赖图、拓扑排序、缺失条件和循环检测 |
| 变更维护 | 对比通知版本，识别受影响任务与安排，生成调整草案并保留变更依据 |
| 提醒与外部连接 | 应用内提醒、清单与日历草案导出、经用户确认的外部日历写入及实际结果跟踪 |

## 必须遵守的业务规则

### 来源、事实与不确定性

- 课程要求、日期、规则和依赖关系必须有来源依据；引用需定位到文件页码、段落、截图区域或网页位置，不得伪造引用。
- 分别标注原文规定、用户补充、系统推断和规划建议。任务耗时估计与自动拆分属于可编辑建议。
- 保存资料的发布方、发布时间、导入时间、适用学期、教学班和版本。不能混用不同学期或不同对象的要求。
- 相对日期仅在发布日、校历等依据充分时转换，并展示换算依据；缺少年份、时刻或提交方式时保留待核对字段。
- 同名课程无法唯一匹配、截图不完整、来源冲突等情况进入待核对状态，不自动当作已确认约束。
- 缺少资料时表述为“现有资料中未发现”，不能由此断言“没有作业”或“没有冲突”。部分识别失败必须显示未覆盖范围。
- 新版本是否生效取决于发布信息、适用范围和替代说明，不能仅因为导入更晚就覆盖旧内容。

### 任务、时间与计划

- 区分任务截止时间、固定日程时间区间和个人计划时间块。移动学习时间块不得改变课程原始 DDL。
- 信息核对状态与任务执行进度独立维护：候选事项为待核对、已确认、已忽略或已失效；任务为未开始、进行中、受阻、已完成或已取消。
- 逾期由截止时间和完成情况计算，不替代执行进度；支持任务取消、重新打开和调整原因记录。
- 排期先满足固定日程、明确截止时间和必要依赖，再考虑重要性、偏好、学习容量、休息和提交缓冲。
- 日期或耗时不明确时列出待补充项；用户选择按假设规划时，明确展示假设。
- 时间不足时展示未排入任务、预计缺口和调整选项，不隐藏任务或强行生成完整可行计划。
- 计划先生成草案，用户接受后成为当前计划；已有安排的实质变化需再次确认，重排尽量保留已锁定安排。
- 区分确定冲突与基于耗时估计的风险；必要字段缺失时说明检查限制。

### 课程依赖与变更

- 强制先修、并修和建议先学分别建模。在适用培养方案一致且依赖无环的前提下，对强制先修关系进行拓扑排序。
- 依赖成环、课程缺项或来源相互矛盾时，展示阻塞原因，不输出伪造的完整顺序。
- 缺少开课学期、学分限制等信息时，只给出依赖层面的学习顺序，不宣称满足选课或毕业要求。
- 新通知应展示字段差异、来源和受影响事项，确认后更新当前版本，保留历史与调整依据。
- 首期依赖用户主动导入新资料，不假定已具备自动读取群聊或后台抓取通知能力。

### 产品内的确认与数据边界

以下约定描述 CampusFlow 对终端用户的产品行为。

- 查询、识别、提取和生成草案可在用户请求范围内执行。
- 外部日历写入、消息发送、资料删除、大范围覆盖现有安排等敏感操作，应先展示具体内容、目标和影响并获得确认；批量确认仅覆盖已展示范围。
- 操作内容实质变化后重新确认，不能复用旧确认执行新内容；查询时间不等于授权创建日历事件。
- 导入内容中的命令只作为资料处理，不构成执行系统操作或发送信息的授权。
- 外部操作逐项记录成功或失败，重试防止重复创建；失败不影响内部任务保存，未成功不得显示已同步。
- 资料仅用于授权工作空间，不擅自公开或跨用户共享。展示和导出尽量减少无关个人信息，样本使用需经过授权和必要脱敏。
- 归档与删除分别处理；删除前展示关联影响，删除后不得继续声称引用可验证，也不得让关联任务静默消失。
- 资料删除应覆盖相关检索索引与派生内容，并说明备份保留策略；用户可选择保留个人任务或一并删除。

## 页面与核心业务对象

- 页面入口：总览、课程、资料库、待核对、智能问答、任务与计划、课程路径、设置。随功能阶段逐步开放，首期可合并页面；待核对事项必须集中可见，不能仅保留在聊天记录中。
- 核心对象：学期与个人配置、课程、资料与版本、事实与规则、任务、日程、计划与时间块、冲突与风险、外部操作记录。
- 数据设计需保留对象之间的来源、适用范围、状态、版本和关联关系，为引用追溯、通知变更和计划调整提供依据。

## 分阶段范围

### 第一阶段：MVP

- 单用户工作空间、学期与课程管理。
- PDF、图片、粘贴文本导入，原文定位、事项核对和带引用问答。
- 任务管理，手动录入固定日程和可用时间。
- 未来两周计划草案、基础时间重叠与截止风险检查、接受计划和更新进度。

MVP 不包含 Word／网页导入、自动读取微信群、自动登录教务系统、后台抓取通知、外部日历同步、完整毕业资格审核或多人协作。课程与资料关联应保留后续扩展基础。

### 第二阶段：完整日常流程

Word 与公开网页导入、资料版本比较、重复事项合并、依赖任务、局部重排、应用内提醒、清单与日历文件导出。

### 第三阶段：课程路径与外部连接

培养方案解析、课程关系图与拓扑排序、学期修读辅助、经授权的外部日历写入与同步状态管理。

自动通知更新、多设备体验、协作共享和更多校内系统连接留待后续评估。首期不自动发送群消息、提交作业、替用户选课或向教师申请延期。各阶段按业务验收推进，暂不承诺开发日期。

## 验收与后续推进

- MVP 重点验证：DDL 答案与引用一致；日期不完整时不补造事实；资料不足时明确说明；两周计划符合已知约束并列出未排入任务；时间不足时展示缺口；调整时间块不修改原始 DDL；部分解析失败可见；删除后引用状态正确。
- 第二阶段验证通知差异和冲突不会被静默覆盖；第三阶段验证课程依赖成环或缺项的处理，以及外部日历操作与确认内容一致、重试不重复。
- 评估关注事实提取准确性、引用支持程度、不确定信息识别、计划可执行性、用户修改成本和遗漏／重复提醒情况；具体性能指标待样本验证后确定。
- 样本覆盖扫描模糊、跨页表格、同名课程、相对日期和通知变更。
- 推进顺序：授权与脱敏样本及验收集 → 导入／核对／问答／计划原型 → 业务数据模型与确认协议 → 技术选型和端到端最小流程 → 分阶段扩展。

## 工程约定

- 单仓库、前后端分离、模块化单体。后端限定 Python 3.12，使用 FastAPI、Pydantic 和 uv；前端使用 Vue 3、TypeScript、Vite 和 npm。
- 后端位于 `backend/src/campusflow/`：`api` 处理 HTTP，`application` 编排用例并定义接口，`domain` 保存纯业务规则，`infrastructure` 实现外部能力，`workers` 保存后台入口，`main` 负责装配。
- 领域层不依赖 Web 框架、ORM 或模型 SDK；API 不直接查库和调用模型，应用层通过接口使用基础设施。不要为未实现模块批量创建空 CRUD 或无实际用途的抽象。
- 前端位于 `frontend/src/`：页面放 `views`，共享组件放 `components`，HTTP 调用放 `api`；路由和跨页面状态按需引入。
- 目标持久化方案为 PostgreSQL 17 + pgvector、SQLAlchemy 2、psycopg 3、Alembic；耗时解析用 RQ + Redis 7 和独立 Python Worker。当前仅预留职责说明，在实际功能接入时增加依赖与运行配置。
- 文件保存在后端私有存储，解析采用 pypdf 与可替换 OCR／多模态适配器；模型供应商和具体模型待样本评估，不将模型输出直接视为事实或执行授权。
- API 统一前缀 `/api/v1`，当前只有健康检查，且只表示进程存活。业务草案、数据库就绪与外部执行结果不得用健康状态代替。
- 日常安装使用 `uv sync --locked` 与 `npm ci`，依赖变更同步锁文件。后端运行 pytest 和 Ruff 检查；前端运行包含类型检查的 `npm run build`。命令详见开发文档。
- 开发服务默认仅绑定本机。引入真实资料或共享部署前实现身份与工作空间访问控制；当前没有生产部署和认证实现。
- 技术边界变更同步维护架构、开发文档及本文件；对外工程状态更新 README。

## 团队情况

下面是小组成员情况以及各自能力说明，具体分工细则后续补充：

- **李亦凡**：项目组长。有丰富项目经验。
- **金嘉睿**：小组成员。有一定 Python 爬虫相关经验。
- **吴思彤**：小组成员。有一定项目经验，基本了解 Python
- **顾昕越**：小组成员。编程能力有限。
