# CampusFlow 贡献指南

本文约定小组成员及后续贡献者如何提交需求、文档和代码。项目当前处于业务规划阶段，开发环境与自动化检查命令将在技术方案落地后补充。

## 开始之前

- 阅读 [README.md](README.md)，了解产品功能、阶段范围和业务验收标准。
- 阅读 [AGENTS.md](AGENTS.md)，了解必须保持的业务规则及团队背景。
- 仓库连接可参考 [GitLab 说明](docs/GitLab.md)。执行远程操作前使用 `git remote -v` 核实配置；远程名称取决于本机设置，不能假定 `origin` 一定是 GitHub 或 GitLab，也不要重复添加已有远程。
- 具体成员分工尚未确定。认领任务时在 Issue 或小组现有任务记录中写明负责人、范围、交付物与验收条件，避免重复修改同一部分。

## 可以贡献什么

除功能代码外，也欢迎需求澄清、页面原型、文档修订、经过授权和脱敏的样本、测试用例与问题复现。任务应尽量拆成可以独立验收的小项。

新增功能应说明属于哪个交付阶段。涉及 MVP 扩展、技术栈选择或业务规则调整时，先在任务记录中形成小组共识，再开展大规模实现。

## 提交需求与问题

功能建议请说明：使用场景、当前问题、期望行为、所属阶段、验收条件。不要只写技术方案而缺少要解决的问题。

问题报告请包含：

- 复现步骤与最小必要输入。
- 预期结果和实际结果。
- 使用的版本或提交编号，以及相关环境信息。
- 经过脱敏的截图或日志；无法复现时说明已尝试的步骤。

涉及密钥、账户凭据或个人资料泄露的问题，不要把敏感内容贴到公开 Issue；通过团队已有的私下渠道联系项目组长处理。

## 分支与提交

1. 开始前检查 `git status`，先妥善保存现有工作，再同步目标仓库的默认分支。
2. 从最新默认分支创建任务分支，一个分支聚焦一项任务。默认分支以实际仓库设置为准，目前本地使用 `main`。
3. 人工开发可采用 `feat/<主题>`、`fix/<主题>`、`docs/<主题>`；Codex 创建的分支默认使用 `codex/<主题>`。
4. 提交前检查差异，仅暂存本次任务涉及的文件，避免将他人的修改或本地资料一起提交。
5. 将任务分支推送到本次协作约定的远程，通过 GitLab Merge Request（MR）或 GitHub Pull Request（PR）请求合并。

提交说明推荐采用 `类型: 具体变化`，正文可使用中文，例如：

```text
docs: 补充两周计划的业务验收标准
feat: 为任务保留原始截止时间和来源
fix: 标记无法确定年份的通知日期
```

常用类型包括 `feat`、`fix`、`docs`、`test`、`refactor` 和 `chore`。保持提交内容聚焦，不把无关格式调整混入功能修改。

避免直接向共享默认分支推送，不强制推送覆盖他人工作。解决冲突时理解双方变更，不直接整份覆盖文件。多远程场景中，明确本次 MR／PR 的目标仓库，不默认向所有远程同步推送；这些约定不代表平台已配置分支保护。

## MR／PR 与评审

提交说明应包含以下内容，可按变更规模简化：

```markdown
## 问题与结果
解决什么问题，修改后的行为是什么？关联哪个任务？

## 变更范围
涉及哪些功能或文档？是否改变业务规则、阶段范围或数据结构？

## 验证
执行了哪些检查，结果如何？哪些内容尚未验证，原因是什么？

## 影响
是否存在兼容性变化、迁移步骤或已知限制？界面变更可附脱敏截图。
```

- 合并前至少由一名其他小组成员检查业务一致性和变更内容，相关评审意见解决后由有权限的成员合并。
- 作者负责理解并解释提交内容，包括 AI 辅助生成的代码、文档和测试。
- 评审关注正确性、可维护性和验收结果，讨论具体问题，尊重不同经验水平的成员。
- 评审后的实质修改应补充验证，并提醒评审者重新查看。

## 验证要求

- 纯文档变更检查链接、Markdown 展示、术语和阶段范围；无需为文字修改增加无意义的代码测试。
- 功能变更按 README 中对应阶段的验收标准验证；缺陷修复在适用时补充能够复现问题的回归测试。
- 日期提取、引用、排期、通知变更和外部写入等功能，应验证正常场景以及相关边界条件，例如缺失字段、冲突来源、时间不足和重复重试。
- 不得将未运行的测试写成通过。仓库尚未具备相关运行环境时，记录可完成的检查和待验证项。
- 技术栈确定后，将安装、运行、测试与格式检查命令加入项目文档，再作为统一检查依据。

## 文档与资料管理

- README 面向外部展示，AGENTS 保存可复用的项目背景和业务约定，本文保存贡献流程。
- 修改业务范围、关键规则或交付阶段时，同步更新 README 与 AGENTS；调整协作流程时更新本文，并检查相关引用。
- 明确区分规划、实现中和已验证的功能，不将设计稿或示例输出描述成已经具备的能力。
- 不提交密钥、密码、访问令牌、真实群聊隐私或未经授权的课程资料。样本优先使用合成数据或获得授权并脱敏的数据。
- 本地配置、依赖目录和生成产物应根据实际技术栈配置忽略规则；不要仅依赖 `.gitignore` 代替提交前检查。
- 引用第三方代码、图片或数据时记录来源，并核对其授权条件；保留需要保留的声明。

## 许可证状态

项目许可证尚待小组决定，目前不创建占位 `LICENSE`，也不声明采用 MIT 等开源许可证。贡献指南本身不构成项目对外使用、修改或分发的授权。

确定许可证后，应补充正式 `LICENSE`、版权信息与适用范围，并同步更新 README 和本文。第三方代码与课程资料的授权需要分别核对，不能默认由项目许可证覆盖。
